const firstName = 'Gerard'
const lastName = 'Lambert'
const city = 'Paris'

// Syntax 1 (template litterals)
console.log(`Your name is ${firstName} ${lastName} and you live in ${city}.`)

// Syntax 2 (concatenation)
console.log('Your name is ' + firstName + ' ' + lastName + ' and you live in ' + city)